<!----->
<div class="col-lg-12">
  <h2>Lista de Asignaciones de Funciones a Usuarios</h2>
  <div class="table-responsive  col-lg-12">
    <table class="table table-hover table-striped tablesorter">
			<thead>
    		<tr>
      		<th>Fecha</th>
      		<th>Es Actual</th>
					<th>Descripcion</th>
					<th>Usuario</th>
      		<th>Rol</th>
      		<th colspan="3"></th>
    		</tr>
  		</thead>
    <tbody>
    <% @asignacion_funciones.to_enum(:each_with_index).each do |asignacion_funcion,i| %>
      <tr>
        <td><%= l asignacion_funcion.created_at %></td>
        <td><%= t asignacion_funcion.esActual.to_s %></td>
        <td><%= asignacion_funcion.descripcion %></td>
				<%if asignacion_funcion.usuario != nil %>
        <td><%= @usuario_rol[i][:usuario] %></td>
				<% else  %>
				<td></td>
				<% end  %>
        <td><%= @usuario_rol[i][:rol]  %></td>
        <td><%= link_to 'Editar', edit_asignacion_funcion_path(asignacion_funcion) %></td>
        <td><%= link_to 'Eliminar', asignacion_funcion, method: :delete, data: { confirm: 'Seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
  </div>
</div>

