<%= form_for(@estado_proyecto) do |f| %>
  <% if @estado_proyecto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@estado_proyecto.errors.count, "error") %> prohibited this estado_proyecto from being saved:</h2>

      <ul>
      <% @estado_proyecto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
