<h1>Listing estado_proyectos</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @estado_proyectos.each do |estado_proyecto| %>
      <tr>
        <td><%= estado_proyecto.nombre %></td>
        <td><%= link_to 'Show', estado_proyecto %></td>
        <td><%= link_to 'Edit', edit_estado_proyecto_path(estado_proyecto) %></td>
        <td><%= link_to 'Destroy', estado_proyecto, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Estado proyecto', new_estado_proyecto_path %>
