<h1>Sistema Estudiantil de Voluntariado Universitario</h1>
  <ol class="breadcrumb">
    <li><i class="icon-dashboard"></i><%= link_to 'Registrarme', new_usuario_path %></li>
  </ol>
<div class="row">
  <div class="col-lg-6">
    <form role="form">
    <div class="form-group">
      <label><h2><small>Nombre de Usuario</small></h2></label>
      <input class="form-control" type="text" value= <%= @contrasenia %> > </input> 
      <label><h2><small>Contraseña</small></h2></label>
      <input class="form-control" type="text" value= <%= @contrasenia %> > </input>  
			<br></br>
			<button type="button" class="btn btn-primary btn-lg">Iniciar Sesión</button>       
    </div>
  </form>
</div>

<table>
  <tbody>
    <% @usuarios.each do |usuario| %>
      <!-- TODO: redimensionar imagen -->
      <td><%= image_tag usuario.foto.url(:thumb) %></td>
        <td><%= usuario.nombreUsuario %></td>
        <td><%= usuario.contrasenia %></td>
        <td><%= usuario.apellido_nombre %></td>
        <td><%= usuario.email %></td>
        <td><%= usuario.direccion %></td>
        <td><%= usuario.fechaRegistro %></td>
        <td><%= link_to 'Show', usuario %></td>
        <td><%= link_to 'Edit', edit_usuario_path(usuario) %></td>
        <td><%= link_to 'Destroy', usuario, method: :delete, data: { confirm: 'Esta seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

