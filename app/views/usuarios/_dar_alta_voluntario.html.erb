<%= simple_form_for(@usuario,
    wrapper_mappings: {
    check_boxes: :horizontal_radio_and_checkboxes,
    radio_buttons: :horizontal_radio_and_checkboxes,
    file: :horizontal_file_input,
    boolean: :horizontal_boolean
  }) do |f| %>
  <% if  @usuario.errors.any? %>
    <div class="col-lg-12">
      <div class="col-lg-3"></div>
        <div id="error_explanation" class="col-lg-6">
          <div class="alert alert-danger">
            <a class="close" data-dismiss="alert">x</a>
            <strong><%= pluralize(@usuario.errors.count, "Error") %> en la creacion del Voluntario:</strong>
            <ul>
              <% @usuario.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    </div>
  <% end %>
  <div class="row" align="center">
    <div class="col-lg-3" align="center"></div>
    <div class="col-lg-6" align="center">
      <div class="form-inputs">
    <%#= f.label :legajo %>
    <%#= f.number_field :legajo %>
    <%# @usuario.type = "Voluntario" %>
    <%= f.input :legajo, label: 'Legajo', placeholder: 'Legajo...', :required => true, :autofocus => true %>
    <%= f.input :type,:as => :hidden, input_html: {value: params[:type]} %>

    <!--<a href="/public/terminos_y_condiciones_SEVU.pdf">Ver términos y condiciones</a>-->
  </div></br></div></div>
  <div class="col-lg-12" align="left">

      <p>Haciendo click en el botón "Confirmar" usted acepta los términos y condiciones del uso del sistema que se detallan a continuación:</p>
      <div>
        <%= link_to content_tag(:i, fa_icon("file-pdf-o ", text: "Ver términos y condiciones")), "/terminos_y_condiciones_SEVU.pdf", :target => "_blank" %>
      </div>
  </div>
</br>
  <div class="actions" align="center">
      <%= submit_tag 'Cancelar', :type => :reset, :class => "btn btn-default", "data-dismiss" => "modal", "aria-hidden" => "true" %>
      <%= f.button :submit, "Confirmar", :class => "btn btn-primary", data: { confirm: "Recuerde que no podrá cambiar su legajo una vez guardado, ¿Desea continuar?" }   %>
    </div>
  <% end %>