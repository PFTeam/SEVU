<!----->
<% if  @error != 0 %>
						<p class="alert alert-dismissable alert-danger">Debe seleccionar voluntarios e items.</p>
<% end %>
<div class="col-lg-6">
	<h2>Reporte de Voluntarios</h2>
</div>
<div class="col-lg-6">
		<br></br>
	<%= form_tag nil, method: :get do %>
	<%= select_year(DateTime.now.year, :start_year => 2000, :end_year =>DateTime.now.year, prefix: :anio_rep) %>
	<%= submit_tag 'Generar Reporte', { :class=>"btn btn-primary btn-lg" }  %>
</div>
<div class="col-lg-12">
	<div class="table-responsive  col-lg-4">
		<h3>Voluntarios Disponibles</h3> <!--IMPLEMENTAR TODO CON VOLUNTARIOS, AHORA ESTA TODO CON USUARIOS--->
		<table class="table table-hover table-striped tablesorter">
			<thead>
         <tr>
           <th class="col-lg-6">Nombre Usuario</th>
					 <th class="col-lg-6">Apellido y Nombre </th>
         </tr>
      </thead>
			<tbody>
			<% Usuario.all.each do |usuario| %>
				<tr>
					<td><%= usuario.nombreUsuario %></td>
					<td><%= usuario.apellido_nombre %></td>					
					<td><%= check_box_tag 'usuario_ids[]', usuario.id %></td>
				</tr>
			<% end %>
			</tbody>
		</table>
	</div>
	
	<div class="table-responsive  col-lg-8">
		<h3>Items Disponibles</h3>
		<table class="table table-hover table-striped tablesorter">
			<thead>
         <tr>
           <th class="col-lg-12">Nombre Item</th>
         </tr>
      </thead>
			<tbody>
				<tr>
					<td>Voluntarios en estado suspendido</td>
					<td><%= check_box_tag 'item_ids[]', 1 %></td>
				</tr>
				<tr>
					<td>Voluntarios en estado activo</td>
					<td><%= check_box_tag 'item_ids[]', 2 %></td>
				</tr>
				<tr>
					<td>Cantidad de actividades en estado creada asociado a cada voluntario</td>
					<td><%= check_box_tag 'item_ids[]', 3 %></td>
				</tr>
				<tr>
					<td>Cantidad de actividades en estado planificada asociado a cada voluntario</td>
					<td><%= check_box_tag 'item_ids[]', 4 %></td>
				</tr>
				<tr>
					<td>Cantidad de actividades en estado ejecutada asociado a cada voluntario</td>
					<td><%= check_box_tag 'item_ids[]', 5 %></td>
				</tr>
				<tr>
					<td>Cantidad de actividades en estado terminada asociado a cada voluntario</td>
					<td><%= check_box_tag 'item_ids[]', 6 %></td>
				</tr>
				<tr>
					<td>Cantidad de actividades en estado cancelada asociado a cada voluntario</td>
					<td><%= check_box_tag 'item_ids[]', 7 %></td>
				</tr>
				<tr>
					<td>Cantidad de horas efectivas realizadas por voluntario</td>
					<td><%= check_box_tag 'item_ids[]', 8 %></td>
				</tr>
			</tbody>
 		</table>
	</div>
</div>
<% end %>
