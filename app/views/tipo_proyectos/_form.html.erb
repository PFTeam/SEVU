<%= form_for(@tipo_proyecto) do |f| %>
  <% if @tipo_proyecto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_proyecto.errors.count, "error") %> prohibited this tipo_proyecto from being saved:</h2>

      <ul>
      <% @tipo_proyecto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_area :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
