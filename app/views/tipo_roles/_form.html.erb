<%= form_for(@tipo_rol) do |f| %>
  <% if @tipo_rol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_rol.errors.count, "error") %> prohibited this tipo_rol from being saved:</h2>

      <ul>
      <% @tipo_rol.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_area :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
