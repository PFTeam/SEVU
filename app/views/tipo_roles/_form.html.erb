<!----->
<%= simple_form_for(@tipo_rol) do |f| %>
  <% if @tipo_rol.errors.any? %>
    <div id="error_explanation">
      <ul>
      <% @tipo_rol.errors.full_messages.each do |message| %>
         <font color="red"><li><%= message %></li></font>
      <% end %>
      </ul>
    </div>
  <% end %>
	<div class="col-lg-6">
		<div class="inputs">
			<%= f.input :nombre, label: 'Nombre de Tipo de Rol', placeholder: 'tipo de rol...', :required => true, :autofocus => true %>
			<br></br> 
			<%= f.input :descripcion, label: 'Descripción del Tipo de Rol', placeholder: 'descripción...', :required => true, :autofocus => true %>
			<br></br>
		</div>
		<div class="actions">
		  <%= f.button :submit, "Aceptar", :class => "btn btn-primary btn-lg" %>
		</div>
	
	<br></br>
 
	</div>
<% end %>
