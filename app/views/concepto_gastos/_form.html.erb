<%= form_for(@concepto_gasto) do |f| %>
  <% if @concepto_gasto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@concepto_gasto.errors.count, "error") %> prohibited this concepto_gasto from being saved:</h2>

      <ul>
      <% @concepto_gasto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br>
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_area :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
