<%= form_for(@evento_publico) do |f| %>
  <% if @evento_publico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@evento_publico.errors.count, "error") %> prohibited this evento_publico from being saved:</h2>

      <ul>
      <% @evento_publico.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_area :descripcion %>
  </div>
  <div class="field">
    <%= f.label :fechaRealizacion %><br>
    <%= f.datetime_select :fechaRealizacion %>
  </div>
  <div class="field">
    <%= f.label :usuario_id %><br>
    <%= f.text_field :usuario_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
