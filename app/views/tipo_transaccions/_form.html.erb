<%= form_for(@tipo_transaccion) do |f| %>
  <% if @tipo_transaccion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_transaccion.errors.count, "error") %> prohibited this tipo_transaccion from being saved:</h2>

      <ul>
      <% @tipo_transaccion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
