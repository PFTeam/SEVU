<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %> alert-dismissible">
    <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    <%= value %>
   </div>
<% end %>


<p>
  <strong>Título:</strong>
  <%= @necesidad.titulo %>
</p>


<p>
  <strong>Descripcion:</strong>
  <%= @necesidad.descripcion %>
</p>


<p>
  <strong>Ambitoaplicacion:</strong>
  <%= @necesidad.ambitoAplicacion %>
</p>

<p>
  <strong>Usuario:</strong>
  <%= @necesidad.usuario %>
</p>

<% if @necesidad.created_at > Time.now - 160.hours and @necesidad.usuario == current_usuario %> 
      <%= link_to 'Editar', edit_necesidad_path(@necesidad) %> | 
<% end %>
<%= link_to 'Atrás', necesidades_path %>
