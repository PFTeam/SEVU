<%= form_for(@estado_usuario) do |f| %>
  <% if @estado_usuario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@estado_usuario.errors.count, "error") %> prohibited this estado_usuario from being saved:</h2>

      <ul>
      <% @estado_usuario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
