<%= form_for(@requisito) do |f| %>
  <% if @requisito.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@requisito.errors.count, "error") %> prohibited this requisito from being saved:</h2>

      <ul>
      <% @requisito.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :actividad_id %><br>
    <%= f.text_field :actividad_id %>
  </div>
  <div class="field">
    <%= f.label :habilidad_id %><br>
    <%= f.text_field :habilidad_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
