<h1>Listing estado_presupuestos</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @estado_presupuestos.each do |estado_presupuesto| %>
      <tr>
        <td><%= estado_presupuesto.nombre %></td>
        <td><%= link_to 'Show', estado_presupuesto %></td>
        <td><%= link_to 'Edit', edit_estado_presupuesto_path(estado_presupuesto) %></td>
        <td><%= link_to 'Destroy', estado_presupuesto, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Estado presupuesto', new_estado_presupuesto_path %>
