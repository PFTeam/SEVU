<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %> alert-dismissible">
    <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    <%= value %>
   </div>
<% end %>

<p>
  <strong>Esactual:</strong>
  <%= @historial_estado_presupuesto.esActual %>
</p>

<p>
  <strong>Fechacambioestado:</strong>
  <%= @historial_estado_presupuesto.fechaCambioEstado %>
</p>

<p>
  <strong>Presupuesto:</strong>
  <%= @historial_estado_presupuesto.presupuesto %>
</p>

<p>
  <strong>Estado presupuesto:</strong>
  <%= @historial_estado_presupuesto.estado_presupuesto %>
</p>

<%= link_to 'Editar', edit_historial_estado_presupuesto_path(@historial_estado_presupuesto) %> |
<%= link_to 'Atrás', historial_estado_presupuestos_path %>
